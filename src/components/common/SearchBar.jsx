function SearchBar(props) {
    return (
        <>
            <div className={props.className}>
                <input  type="text" />
            </div>
        </>
    )
};

export default SearchBar;